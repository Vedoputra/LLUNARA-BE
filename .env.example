# Server
PORT=8080
ENV=development
ALLOWED_ORIGINS=

# Supabase (lihat BE-1.1 — diisi setelah project Supabase dibuat)
# Gunakan connection string dari connection pooler, bukan direct connection.
DATABASE_URL=
SUPABASE_URL=
SUPABASE_JWT_SECRET=

# Hanya untuk operasi admin (mis. hapus akun di BE-6.4). TIDAK PERNAH dipakai di frontend.
SUPABASE_SERVICE_ROLE_KEY=
